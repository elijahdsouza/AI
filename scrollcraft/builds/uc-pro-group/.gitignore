# No trailing slash: with "node_modules/" git only ignores a directory, and
# the local playwright install here is a symlink, which slipped through.
node_modules
lab/serve.log

# Full-page contact sheets. Multi-megabyte, regenerable in one command
# (node lab/sheet3.mjs), and they were pushing .git past 24MB. The
# per-section screenshots stay tracked as verification evidence.
lab/v3-FULL.png
lab/v3-sheet-*.png
lab/print-*.png
